- name: Deploy Nginx
  hosts: all
  become: true
  tasks:

    - name: Install Nginx
      yum:
        name: nginx
        state: present

    - name: Start Nginx
      service:
        name: nginx
        state: started
        enabled: yes

    - name: Display Nginx server IP
      debug:
        var: ansible_default_ipv4.address
